# Copy to .env and adjust if your backend isn't on the default port.
# NEVER put GROQ_API_KEY or GOOGLE_API_KEY here -- Vite bundles every
# VITE_* var into browser-readable JS. Secrets stay in backend/.env only.
VITE_API_BASE_URL=http://localhost:8000
