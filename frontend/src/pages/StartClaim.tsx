/** Day 3 scaffold stub: file drop + LOB hint, POST /api/claims, redirect to ProcessingView. */
export default function StartClaim() {
  return (
    <div className="flex h-screen items-center justify-center text-sm text-slate-400">
      Start Claim -- built in Day 3.
    </div>
  );
}
