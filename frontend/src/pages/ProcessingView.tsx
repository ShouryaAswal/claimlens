/** Day 3 scaffold stub: SSE-driven stage list, transitions to ClaimReview on pipeline.done. */
export default function ProcessingView() {
  return (
    <div className="flex h-screen items-center justify-center text-sm text-slate-400">
      Processing View -- built in Day 3.
    </div>
  );
}
