# --- Sprint 0 / Sprint 1 (in use today) ---
pydantic>=2.0
pymupdf
reportlab
pytest

# --- Ingestion robustness (multi-format: scanned PDF, images, docx, URLs) ---
pytesseract       # requires the `tesseract-ocr` system package (see README)
pdf2image         # requires the `poppler-utils` system package (see README)
python-docx
pillow
requests

# --- Corpus generation (scanned-PDF test fixture) ---
img2pdf
numpy

# --- Sprint 2 (local LLM extraction) ---
# ollama          # pip client for a locally running Ollama server
# httpx

# --- Sprint 4 (UI) ---
# streamlit

# --- Optional: VLM-based OCR upgrade path (not wired up, see README) ---
# paddleocr
# paddlepaddle
